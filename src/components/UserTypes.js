import React from "react";
import { Text, View } from "react-native";

const UserTypes = () => {
  return (
    <View>
      <Text style={{ opacity: 0.7, color: "grey" }}>User types message</Text>
    </View>
  );
};

export default UserTypes;
